export default app -->you are exporting a single value, function, or component named "app" as the primary (default) export from a module, allowing you to import it into other parts of your code without needing to specify a specific name when importing


"bcrypt":bcrypt is a password-hashing library used to securely store user passwords.

"cloudinary": Cloudinary is a cloud-based media management platform designed to handle the entire lifecycle of images, videos, and other rich media assets for web and mobile applications.

"cookie-parser": Helps with authentication
"cors": CORS is a security feature implemented by browsers to restrict web pages from making requests to domains other than the one that served the web page. By using the cors module, you can configure your server to allow or restrict cross-origin requests
"dotenv":   
"express-fileupload": 
"jsonwebtoken": 
"node-cron": 
"nodemailer": 


In an online auction system, unpaid commissions refer to scenarios where a platform does not charge a fee or percentage of the sale price from sellers or buyers. This model has both advantages and disadvantages for different stakeholders, including the platform, sellers, and buyers. 


cloudinary.v2.config()-->	Sets these parameters globally for all Cloudinary operations in your application.

mimetype-->When you upload or download a file, the MIME type helps the receiving system understand what kind of file it is and how to handle it.